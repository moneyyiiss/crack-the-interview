export default {
  id: 'interview',
  title: 'Interview Practice',
  color: '#E11D48',
  topics: [
    {
      title: "Top 'Explain the Difference' Traps",
      points: [
        '== vs equals(): reference identity vs content comparison.',
        'ArrayList vs LinkedList: array-backed random access vs node-linked cheap ends.',
        'Abstract class vs Interface: shared state/code vs pure contract (default methods since 8).',
        'Checked vs Unchecked exceptions: compiler-enforced vs not.',
        'HashMap vs Hashtable: not synchronized/allows null vs synchronized/no nulls.',
        'Overloading vs Overriding: compile-time, same class vs runtime, parent-child.',
      ],
      qa: [],
      code: '// Quick mental checklist before an interview:\n// == -> reference | equals() -> content\n// ArrayList -> array | LinkedList -> nodes\n// Abstract class -> state+code | Interface -> contract\n// Checked -> compiler enforced | Unchecked -> not\n// HashMap -> not sync, 1 null key | Hashtable -> sync, no nulls\n// Overload -> compile-time | Override -> runtime',
      flow: { type: 'grid', items: [
        { label: '== vs equals()', sub: 'identity vs content' },
        { label: 'ArrayList vs LinkedList', sub: 'array vs nodes' },
        { label: 'Overload vs Override', sub: 'compile vs runtime' },
        { label: 'HashMap vs Hashtable', sub: 'unsync vs sync' },
      ] },
    },
    {
      title: 'Common Coding-Round Patterns',
      points: [
        'Two-pointer technique — for sorted arrays/strings (pair sum, reversing, removing duplicates in-place).',
        'Sliding window — for substring/subarray problems with a size or sum constraint.',
        'HashMap for O(1) lookups — turns many O(n²) brute-force problems (two-sum, frequency counting) into O(n).',
        'Recursion + memoization (top-down DP) or bottom-up DP tables for overlapping-subproblem problems (Fibonacci, knapsack, edit distance).',
        'BFS/DFS on a graph or tree — level-order traversal, shortest path in unweighted graphs, detecting cycles.',
      ],
      qa: [],
      code: '// Two-sum with HashMap — O(n), classic interview pattern\nstatic int[] twoSum(int[] nums, int target) {\n    Map<Integer,Integer> seen = new HashMap<>();\n    for (int i = 0; i < nums.length; i++) {\n        int need = target - nums[i];\n        if (seen.containsKey(need)) return new int[]{seen.get(need), i};\n        seen.put(nums[i], i);\n    }\n    return new int[]{-1, -1};\n}',
      flow: { type: 'pipeline', steps: ['Two-pointer', 'Sliding window', 'HashMap lookups', 'Recursion + memo', 'BFS/DFS'] },
    },
    {
      title: "Core Java Questions You'll Almost Certainly Get",
      points: [
        'Explain JVM, JRE, JDK and how a Java program runs end to end.',
        'Why is Java platform-independent? What role does bytecode play?',
        'Explain the four pillars of OOP with real examples, not just definitions.',
        'What happens in memory when you create an object — heap vs stack?',
        'Difference between String, StringBuilder, StringBuffer and why immutability matters.',
      ],
      qa: [],
      code: '// Be ready to draw this on a whiteboard from memory:\n// Main.java -> javac -> Main.class (bytecode) -> JVM (ClassLoader, Verifier,\n// Execution Engine) -> runs on any OS with a JVM installed\n\n// And this:\n// Object obj = new Object();  -> "obj" reference lives on the STACK\n//                              -> the actual Object instance lives on the HEAP',
      flow: { type: 'grid', items: [
        { label: 'JVM/JRE/JDK', sub: 'relationship' },
        { label: 'Bytecode', sub: 'platform independence' },
        { label: '4 pillars', sub: 'with real examples' },
        { label: 'Heap vs Stack', sub: 'object creation' },
      ] },
    },
    {
      title: 'Behavioral + Technical Mix: How to Answer Well',
      points: [
        'Use the STAR method (Situation, Task, Action, Result) for behavioral questions — keeps answers structured and outcome-focused.',
        'When explaining a technical concept, start with a one-line definition, then a concrete code example, then a "why it matters" — interviewers reward clarity over jargon.',
        "If you don't know something, say so honestly and reason through it out loud rather than guessing silently — interviewers often care more about your thought process than a perfect memorized answer.",
        'Always ask clarifying questions on coding problems before writing code — constraints, edge cases, expected input size change the right approach.',
      ],
      qa: [],
      code: '// A good technical answer structure:\n// 1. One-line definition\n// 2. Small code snippet\n// 3. Why it matters / when to use it / common gotcha\n\n// Example: "What is polymorphism?"\n// 1. "One interface, many implementations — resolved at compile-time (overloading)\n//     or runtime (overriding)."\n// 2. Animal a = new Dog(); a.speak(); // calls Dog\'s version at runtime\n// 3. "This is why we can write code against an interface/abstract type and swap\n//     implementations without touching the calling code."',
      flow: { type: 'pipeline', steps: ['Definition', 'Code example', 'Why it matters', 'Common gotcha'] },
    },
    {
      title: 'Common Java Coding Problems (with Approach)',
      points: [
        'Reverse a String / check palindrome — two-pointer from both ends, or StringBuilder.reverse().',
        'Find duplicates in an array — HashSet for O(n) time, or sort + adjacent-compare for O(n log n) with O(1) extra space.',
        'Check if two Strings are anagrams — sort both and compare, or count character frequencies with an int[26]/HashMap.',
        'Find the first non-repeating character — LinkedHashMap (preserves order) counting frequencies in one pass, then a second pass to find count == 1.',
        'Implement a basic LRU cache — LinkedHashMap with accessOrder=true and an overridden removeEldestEntry().',
      ],
      qa: [],
      code: 'static boolean isAnagram(String a, String b) {\n    if (a.length() != b.length()) return false;\n    int[] counts = new int[26];\n    for (char c : a.toCharArray()) counts[c - \'a\']++;\n    for (char c : b.toCharArray()) counts[c - \'a\']--;\n    for (int c : counts) if (c != 0) return false;\n    return true;\n}',
      flow: { type: 'grid', items: [
        { label: 'Reverse/Palindrome', sub: 'two-pointer' },
        { label: 'Duplicates', sub: 'HashSet O(n)' },
        { label: 'Anagram', sub: 'frequency count' },
        { label: 'LRU cache', sub: 'LinkedHashMap' },
      ] },
    },
    {
      title: 'Final Quick-Revision Cheat Sheet',
      points: [
        'JVM runs bytecode; JRE = JVM + libraries; JDK = JRE + dev tools.',
        'String is immutable; StringBuilder for mutation in loops.',
        'ArrayList = array-backed, fast random access; LinkedList = node-linked, fast at ends.',
        'HashMap = fast, unordered; LinkedHashMap = insertion order; TreeMap = sorted.',
        'Checked exceptions must be declared/caught; unchecked (RuntimeException) don\'t.',
        'Overloading = compile-time, same class; Overriding = runtime, parent-child.',
        'synchronized/Lock for mutual exclusion; volatile for visibility only; Atomic classes for lock-free compound ops.',
        'Streams are lazy until a terminal operation; Optional signals "may be absent" as a return type.',
        'Favor composition over inheritance; program to interfaces, not implementations.',
      ],
      qa: [],
      code: '// Print this mentally before walking into the room:\n// JVM/JRE/JDK -> bytecode portability\n// String immutability -> String pool, thread-safety, safe HashMap keys\n// Collections -> pick by: order needed? duplicates? thread-safety? sorted?\n// Exceptions -> checked (recoverable) vs unchecked (bug)\n// Concurrency -> synchronized (mutual exclusion) vs volatile (visibility) vs Atomic (lock-free)\n// Java 8+ -> Streams (lazy pipelines), Optional (absence), lambdas (functional interfaces)',
      flow: { type: 'pipeline', steps: ['Core & OOP', 'Collections', 'Exceptions', 'Concurrency', 'Java 8+'] },
    },
  ],
};
