// CATEGORIES is loaded as a plain global script (see index.html: <script src="/data.js">)
// so this file's actual data content is never duplicated in the React source —
// it's the same data.js already served at the site root.
export default window.CATEGORIES;
