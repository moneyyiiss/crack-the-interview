export default {
  id: 'intro',
  title: 'Introduction',
  color: '#2F5FDE',
  topics: [
    {
      title: 'What is Java',
      points: [
        'Java is a class-based, object-oriented, platform-independent language — source compiles to bytecode (.class), which the JVM runs on any OS.',
        '"Write once, run anywhere": the same .class file runs on Windows, Linux, macOS as long as a JVM is installed there.',
        'Statically typed and compiled (unlike Python/JS) — many errors are caught before the program ever runs.',
        'Automatic memory management via garbage collection — no manual malloc/free like C/C++.',
        'Widely used for backend/enterprise systems, Android apps, and large-scale distributed systems.',
        'Originally created by James Gosling at Sun Microsystems (1995), now owned and developed by Oracle; the reference implementation is open-sourced as OpenJDK.',
      ],
      qa: [
        { q: 'Is Java 100% platform-independent?', a: "The JVM itself is platform-specific (a different JVM binary per OS); the bytecode it runs is what's portable." },
        { q: 'Is Java compiled or interpreted?', a: 'Both — javac compiles source to bytecode, then the JVM interprets/JIT-compiles that bytecode at runtime.' },
      ],
      code: 'public class Main {\n    public static void main(String[] args) {\n        System.out.println("Hello, Java!");\n    }\n}\n// javac Main.java  -> compiles to Main.class (bytecode)\n// java Main         -> JVM loads & runs it, anywhere Java runs',
      flow: { type: 'pipeline', steps: ['Main.java (source)', 'javac (compiler)', 'Main.class (bytecode)', 'JVM (any OS)', 'Program output'] },
    },
    {
      title: 'Features of Java',
      points: [
        'Simple — syntax close to C/C++ but drops pointers, operator overloading, and manual memory management.',
        'Object-Oriented — everything (except primitives) is modeled as objects; supports the four pillars: encapsulation, inheritance, polymorphism, abstraction.',
        'Platform-independent & portable — bytecode runs unmodified on any device with a JVM.',
        'Robust — strong compile-time type checking, exception handling, and no explicit pointer arithmetic reduce runtime crashes.',
        'Secure — bytecode verifier, classloader sandboxing, and no direct memory access make it hard to write memory-corrupting exploits.',
        'Multithreaded, high-performance (via JIT), and dynamic (classes load and link at runtime, not all at compile time).',
      ],
      qa: [{ q: 'Why is Java called "robust"?', a: 'Because of strong compile-time checking, runtime exception handling, and automatic memory management — programs are less likely to crash from common C/C++-style bugs like dangling pointers.' }],
      code: '// Robustness example: the compiler forces you to handle a checked exception\nvoid readConfig() throws java.io.IOException {\n    java.nio.file.Files.readAllLines(java.nio.file.Path.of("app.config"));\n}\n// No raw pointers: an array access is bounds-checked automatically\nint[] arr = new int[3];\n// arr[5] would throw ArrayIndexOutOfBoundsException, not corrupt memory',
      flow: { type: 'grid', items: [
        { label: 'Simple', sub: 'No pointers' },
        { label: 'Object-Oriented', sub: '4 pillars' },
        { label: 'Portable', sub: 'WORA' },
        { label: 'Secure', sub: 'Sandboxed JVM' },
        { label: 'Robust', sub: 'Strong typing' },
        { label: 'Multithreaded', sub: 'Built-in' },
      ] },
    },
    {
      title: 'JDK, JRE & JVM',
      points: [
        'JVM (Java Virtual Machine) — runs the bytecode; provides memory management, garbage collection, security.',
        'JRE (Java Runtime Environment) — JVM + core libraries; enough to run Java programs, not to compile them.',
        'JDK (Java Development Kit) — JRE + compiler (javac), debugger, and dev tools; needed to write and build Java programs.',
        'Relationship: JDK ⊃ JRE ⊃ JVM.',
        "Since Java 9, JRE isn't shipped separately by default — you install a JDK even just to run programs, or build a minimal custom runtime with jlink.",
      ],
      qa: [{ q: 'Can you run a .class file with only a JRE?', a: 'Yes, JRE is sufficient to run compiled bytecode; you need the JDK only to compile source.' }],
      code: '// JDK  = JRE + development tools (javac, javadoc, jar, debugger)\n// JRE  = JVM + core class libraries (java.lang, java.util, ...)\n// JVM  = the engine that loads .class files and executes bytecode\n\n// You code against the JDK, ship the JRE (or a custom runtime\n// image built with jlink), and the JVM is what actually runs it.',
      flow: { type: 'tree', root: 'JDK', children: [
        { label: 'JRE', sub: 'included inside JDK' },
        { label: 'javac, javadoc, jar', sub: 'dev tools (JDK only)' },
      ] },
    },
    {
      title: 'How Java Code Runs',
      points: [
        'javac MyClass.java → produces MyClass.class (bytecode).',
        "java MyClass → JVM's class loader loads the class, bytecode verifier checks it's safe, then the interpreter/JIT executes it.",
        'JIT (Just-In-Time) compiler converts hot bytecode paths into native machine code at runtime for speed.',
        'main() method signature must be exactly: public static void main(String[] args) — this is the JVM\'s fixed entry point.',
      ],
      qa: [{ q: 'Why must main be static?', a: 'So the JVM can call it without first creating an object of the class.' }],
      code: '// 1. Source code\nclass Greeter { void hi(){ System.out.println("hi"); } }\n\n// 2. javac compiles it to platform-independent bytecode\n//    -> Greeter.class\n\n// 3. JVM\'s class loader reads the .class file into memory\n\n// 4. Bytecode verifier checks it\'s safe\n\n// 5. Interpreter runs it line by line; the JIT compiler\n//    recompiles "hot" methods straight to native machine code',
      flow: { type: 'pipeline', steps: ['Class Loader', 'Bytecode Verifier', 'Interpreter', 'JIT Compiler (hot paths)', 'Native execution'] },
    },
    {
      title: 'Setting Up & Compiling: javac, java, classpath',
      points: [
        'javac Foo.java compiles one or more source files; use javac -d out Foo.java to place .class files in a specific output directory.',
        'java Foo runs the compiled class — note: no ".class" extension, and the class must contain the entry-point main method.',
        'The classpath tells the JVM where to look for compiled classes/JARs: java -cp lib/*.jar:out Foo (";" separator on Windows, ":" on Linux/macOS).',
        'A single source file can be run directly since Java 11 with java Foo.java — the JVM compiles it in-memory and runs it, handy for quick scripts.',
        'Package declaration (package com.example;) must match the folder structure — com.example.Foo lives at com/example/Foo.java.',
      ],
      qa: [{ q: 'Why does java Foo fail with "could not find or load main class" even though compilation succeeded?', a: "Usually a classpath mismatch — you're running from the wrong directory, the package declaration doesn't match the folder layout, or the compiled .class isn't on the classpath you passed with -cp." }],
      code: '// Compile:\n// javac -d out src/com/example/Foo.java\n// Run (classpath = out):\n// java -cp out com.example.Foo\n\n// Since Java 11 — run a single file directly without a separate compile step:\n// java Foo.java',
      flow: { type: 'pipeline', steps: ['Foo.java', 'javac -d out', 'out/Foo.class', 'java -cp out Foo'] },
    },
    {
      title: 'Java Editions, Versions & Release Cadence',
      points: [
        'Three editions: Java SE (Standard Edition, the core language), Java EE / Jakarta EE (enterprise: servlets, JPA — now under the Eclipse Foundation), Java ME (Micro Edition, embedded/mobile — largely legacy today).',
        'Since Java 9, a strict 6-month release cadence: a new feature version ships every March and September, whether or not big features are ready.',
        'LTS (Long-Term Support) releases — Java 8, 11, 17, 21 — get years of vendor support; non-LTS versions are best for trying new features early, not for long-lived production systems.',
        'Java is open-sourced primarily under OpenJDK; Oracle JDK and OpenJDK builds have been functionally near-identical since Java 11.',
        "'Java version' vs 'source/target language level' — you can compile against an older language level while still running on a newer JVM, for compatibility.",
      ],
      qa: [{ q: 'Why do most companies standardize on an LTS version like Java 17 rather than the latest release?', a: 'LTS versions get multiple years of security patches and vendor support, while non-LTS versions stop receiving updates as soon as the next release ships six months later — production systems need that longer support window.' }],
      code: '// Naming: Java 8, 11, 17, 21 ... (the old "1.8" style is gone since 9)\n// LTS (Long-Term Support) releases: 8, 11, 17, 21 — safe defaults for prod\n// Non-LTS releases ship every 6 months with preview/incubator features\n\n// Checking the running version:\npublic class VersionCheck {\n    public static void main(String[] args) {\n        System.out.println(Runtime.version());       // e.g. 21.0.2\n        System.out.println(System.getProperty("java.version"));\n    }\n}',
      flow: { type: 'pipeline', steps: ['Java 8 (LTS)', 'Java 11 (LTS)', 'Java 17 (LTS)', 'Java 21 (LTS)'] },
    },
  ],
};
